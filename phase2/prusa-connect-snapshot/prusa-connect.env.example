# Example ONLY (no real secrets)
# Copy this to: /etc/prusa-connect-snapshot/prusa-connect.env
# and fill in your real values on the Pi (NOT in git).

PC_PRINTER_ID=YOUR_PRINTER_ID_HERE
PC_TOKEN=YOUR_TOKEN_HERE
PC_FINGERPRINT=YOUR_FINGERPRINT_HERE

# Snapshot URL that the uploader pulls from (LAN-only via localhost service)
SNAPSHOT_URL=http://127.0.0.1:8081/snapshot.jpg

# Interval (seconds)
SNAPSHOT_INTERVAL=10
