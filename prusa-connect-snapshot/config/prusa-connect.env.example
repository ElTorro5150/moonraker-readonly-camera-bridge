# Prusa Connect snapshot uploader configuration
# This file is an example. Copy it to prusa-connect.env and edit values.

# Prusa Connect camera token (from Prusa Connect UI)
PC_TOKEN=PUT_YOUR_TOKEN_HERE

# Stable unique identifier for this camera (generate once and keep it)
PC_FINGERPRINT=PUT_YOUR_FINGERPRINT_HERE

# Local MJPEG stream URL (camera is owned by rpicam-vid)
MJPEG_URL=http://127.0.0.1:8081/stream.mjpg

# Minimum interval between uploads (seconds)
INTERVAL_SECONDS=10

# Where the latest snapshot JPEG will be written
JPEG_PATH=/run/prusa-connect/latest.jpg

# Chat GPT 5.2 said this line is not required but it's nice. I bet it causes a crash.
SNAP_URL=http://127.0.0.1:8081/snapshot.jpg
