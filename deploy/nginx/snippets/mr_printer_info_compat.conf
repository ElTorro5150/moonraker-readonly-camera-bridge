# Compatibility for older clients (MobileRaker) that call /printer/info
location = /printer/info {
    default_type application/json;
    return 200 '{"result":{"state":"ready","message":"compat"}}';
}
